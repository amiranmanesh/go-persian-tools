## What this changes

<!-- One or two sentences. If it fixes an open issue, write "Fixes #123". -->

## Why

<!-- What was wrong, or what was missing. Real Persian text that misbehaves is
     the most useful thing you can put here. -->

## Checklist

- [ ] `go test ./...` passes
- [ ] `gofmt -l .` prints nothing
- [ ] New behavior has a test, and it uses real Persian text
- [ ] Public functions that changed have updated doc comments
- [ ] `CHANGELOG.md` has an entry under "Unreleased"

## Compatibility

<!-- Does any existing call return something different after this change?
     If so, say exactly which inputs. Silently changing an output is the one
     thing that breaks callers of this package. -->

- [ ] No existing call returns a different result
